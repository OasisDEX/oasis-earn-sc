{
  "name": "@oasisdex/addresses",
  "version": "1.0.0",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "yarn clean && yarn tsc -p tsconfig.json",
    "clean": "rm -rf ./lib",
    "watch": "yarn tsc -b -w",
    "lint": "yarn tsc -p tsconfig.json",
    "prepublish": "yarn build"
  },
  "dependencies": {
    "@oasisdex/dma-deployments": "1.0.0"
  },
  "files": [
    "lib"
  ]
}
