{
  "name": "@oasisdex/addresses",
  "version": "1.0.0",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf ./lib && yarn tsc -b",
    "watch": "yarn tsc -b -w",
    "lint": "yarn tsc -p ./tsconfig.json --noEmit",
    "prepublish": "yarn build"
  },
  "dependencies": {
    "@oasisdex/dma-deployments": "*"
  }
}
