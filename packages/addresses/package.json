{
  "name": "@oasisdex/addresses",
  "version": "1.0.0",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf ./lib && tsc --declaration && webpack",
    "watch": "yarn tsc -b -w",
    "lint": "yarn tsc -p ./tsconfig.json --noEmit",
    "prepublish": "yarn build"
  },
  "dependencies": {
    "@oasisdex/dma-deployments": "*"
  },
  "sideEffects": false,
  "devDependencies": {
    "@babel/core": "^7.21.4",
    "@babel/preset-env": "^7.21.4",
    "ts-loader": "^9.4.2",
    "webpack": "^5.79.0",
    "webpack-cli": "^5.0.1"
  }
}
