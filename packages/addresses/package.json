{
  "name": "@oasisdex/addresses",
  "version": "1.0.0",
  "main": "src/index.ts",
  "module": "lib/index.min.js",
  "typings": "src/index.ts",
  "license": "MIT",
  "scripts": {
    "build": "yarn clean && yarn tsc -p tsconfig.json && webpack",
    "clean": "rm -rf ./lib",
    "watch": "yarn tsc -b -w",
    "lint": "yarn tsc -p tsconfig.json",
    "prepublish": "yarn build"
  },
  "devDependencies": {
    "@oasisdex/dma-deployments": "*"
  },
  "files": [
    "lib"
  ]
}
