{
  "name": "@oasisdex/dma-library-internal",
  "version": "0.2.16",
  "typings": "dist/index.d.ts",
  "types": "dist/index.d.ts",
  "main": "dist/index.js",
  "targets": {
    "main": {
      "optimize": true
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "bundle": "npx parcel build ./src/index.ts --dist-dir ./dist --no-source-maps",
    "lint": "yarn eslint . --ext .ts --max-warnings=0 && yarn tsc --build ./tsconfig.lint.json",
    "lint:fix": "yarn eslint . --fix --ext .ts"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "bignumber.js": "^9.0.1",
    "ethers": "5.7.2"
  },
  "devDependencies": {
    "@parcel/transformer-typescript-tsc": "^2.8.3",
    "@parcel/validator-typescript": "^2.8.3",
    "parcel": "2.8.3",
    "typescript": ">=3.0.0"
  }
}
