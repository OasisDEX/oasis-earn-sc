query GetPosition($id: ID!) {
  earnPosition(id: $id) {
    oasisEvents(first: 10000, orderBy: blockNumber, orderDirection: asc) {
      kind
      blockNumber
      timestamp
      collateralToken {
        address
        symbol
        decimals
      }
      debtToken {
        address
        symbol
        decimals
      }
      swapToToken
      swapToAmount
      swapFromToken
      swapFromAmount
      collateralBefore
      collateralAfter
      collateralDelta
      debtBefore
      debtAfter
      debtDelta
    }
  }
}
