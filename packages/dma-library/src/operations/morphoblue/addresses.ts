import { Address } from '@deploy-configurations/types/address'
import { TokenAddresses } from '@dma-library/operations/aave-like/addresses'

export interface MorphoBlueStrategyAddresses {
  morphoblue: Address
  tokens: TokenAddresses
}
